# 项目说明文档
## 一、项目简介

本项目是一个**基于 epoll + 主从 Reactor（Main-Sub Reactor）多线程模型的 TCP 聊天服务器**。
- 支持**用户注册/登录**、**私聊**、**群聊**、**好友系统**、**文件传输**等功能。
- 实现了**自定义二进制帧协议**（定长包头 + JSON 消息体 + 可选文件数据），能够正确处理 TCP **粘包 / 半包**。
- 采用**单例模式**管理多个全局模块（账户、群组、会话、UID 分配器）；好友/群组列表的**社交关系模块**则由每个登录会话直接持有，不再单例。
- 使用 **C++17** 与 **nlohmann/json** 解析网络消息。

> 本仓库仅包含**服务器端**源代码，客户端需要自行实现或使用现有测试客户端连接调试。

---

## 二、技术栈

| 项目 | 说明 |
|------|------|
| 开发语言 | C++17 |
| 网络模型 | epoll + 主从 Reactor 多线程 |
| 并发模型 | 1 个 Main Reactor（accept）+ 3 个 Sub Reactor（IO 事件）+ 线程池（8 线程处理业务） |
| JSON 库 | nlohmann/json |
| 构建工具 | CMake |
| 数据持久化 | MySQL Connector/C++（`sql/create_table.sql` 已建表；repo 分层设计见 §6.7，持久化实现待完成） |

---

## 三、目录结构

```
server/
├── CMakeLists.txt                    # CMake 构建配置
├── src/
│   ├── main.cpp                      # 服务器入口：创建 socket、主从 Reactor、线程池
│   ├── net_core/                     # 网络核心层
│   │   ├── epoller.h/.cpp            # 主从 Reactor：main_reactor / sub_reactor
│   │   ├── connection.h/.cpp         # 纯网络收发层（3b 重拆；receiver/sender/帧解析/打包/接收驱动）
│   │   ├── client_session.h/.cpp     # 单个连接会话：登录、私聊、群聊、文件等业务
│   │   ├── message_handler.h/.cpp    # 消息策略分发（多态，Chat/Group/Base/File）
│   │   ├── notice_service.h/.cpp     # 全局通知服务（业务层发消息的"端口"）
│   │   ├── receiver_sender.h/.cpp    # 帧协议解析与发送（sender / receiver）
│   │   ├── server_config.h/.cpp      # 服务器配置（心跳等，读 configure.json）
│   │   └── session_manager.h/.cpp    # 全局会话管理（单例，UID/句柄映射）
│   ├── logic/                        # 业务逻辑层
│   │   ├── account.h/.cpp            # 用户账户信息（会话模块，被 client_session 持有）
│   │   ├── group.h/.cpp             # 群聊
│   │   ├── group_manager.h/.cpp      # 群聊管理（单例，维护在线群的活跃生命周期）
│   │   ├── social_module.h/.cpp      # 社交关系模块（好友/群组列表，会话模块）
│   │   └── UID_allocator.h/.cpp      # 用户/群聊ID分配器（单例）
│   ├── db/                            # 数据库相关（三层：mysql / repo_interface / repo）
│   │   ├── mysql/                     # MySQL 连接池（mysql_conn_pool）
│   │   ├── repo_interface/            # 仓储接口 / 契约（纯接口 + 空实现占位，无 SQL）
│   │   │   ├── account_repository.h/.cpp
│   │   │   ├── friend_repository.h/.cpp
│   │   │   └── group_repository.h/.cpp
│   │   └── repo/                      # 仓储实现（真正的 MySQL 实现，SQL 只在此目录）
│   │       ├── account_repo.h/.cpp     # 账户仓储实现
│   │       ├── friend_repo.h/.cpp      # 好友关系仓储实现
│   │       ├── friend_request_repo.h/.cpp
│   │       ├── group_repo.h/.cpp
│   │       ├── group_member_repo.h/.cpp
│   │       └── message_repo.h/.cpp
│   └── utils/
│       └── thread_pool.h/.cpp        # 线程池
├── include/
│   └── total.h                       # 公共头文件（类型、常量、全局声明）
├── configure.json                    # 服务器配置（use_heartbeat / heartbeat_interval / database）
├── sql/
│   └── create_table.sql              # 数据库建表脚本（Account/Group/message 等）
├── 数据库设计.txt                     # 数据库表结构与 UID 分配设计说明
├── build/                            # 构建输出目录（已配置过 CMake）
├── 客户端接口文档.txt                  # 前后端 JSON/帧协议接口说明
└── 开发日志.txt                       # 待办清单与开发日志
```

---

## 四、启动与编译

### 4.1 依赖

- CMake ≥ 3.10
- 支持 C++17 的编译器
- nlohmann/json 库（`sudo apt install nlohmann-json3-dev`）
- MySQL Connector/C++（用于 `db/mysql` 连接池与 `db/repo`，仅在需要访问数据库的文件中按需引入，`total.h` 不再强制依赖 `<mysql/jdbc.h>`；构建需含 MySQL JDBC 头目录 `/usr/include/mysql-cppconn/jdbc`，已配置在 CMakeLists.txt）

### 4.2 首次配置与编译

```bash
# 第一次：把配置文件复制到运行目录（见下方说明）
cp configure.json build/

# 第一次：配置
cd build
cmake ..

# 编译（此后每次改动只需执行这一条）
cd build
cmake --build .
```

### 4.3 运行服务端

```bash
cd build
./server
```

服务器启动后监听** IPv6 通配地址** `:8080`。

> ⚠️ **关于配置文件**：服务器启动时读取**相对路径** `configure.json`（即当前工作目录下的文件）。程序通常从 `build/` 目录运行，因此请先执行 `cp configure.json build/`（或在 build/ 下创建该文件）再运行，否则只读到默认值（如连接池默认 4、连接信息为默认值），可能连不上你的数据库。配置文件支持心跳与 MySQL 连接池两个部分。详见 `readme.md`。

> 说明：项目使用 IPv6 socket（`AF_INET6`），`in6addr_any` 兼容接收 IPv4 与 IPv6 连接。

---

## 五、架构与工作流程

### 5.1 主从 Reactor 模型

```
                    ┌────────────────────┐
                    │  main_reactor        │
                    │  (epoll_wait)        │
                    │  accept 新连接        │
                    └─────────┬──────────┘
                              │ 按负载轮流分配
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
      ┌────────────┐  ┌────────────┐  ┌────────────┐
      │  sub1       │  │  sub2       │  │  sub3       │
      │  epoll loop │  │  epoll loop │  │  epoll loop │
      └─────┬──────┘  └─────┬──────┘  └─────┬──────┘
            │  直接面向 connection           │
            └───────────────┼───────────────┘
                            ▼
                   ┌──────────────┐
                   │ connection:   │  持有接收缓冲/帧解析/发送
                   │ ThreadPool    │  (8 workers) process_incoming → session
                   └──────────────┘
```

- **main_reactor**：负责 `accept`，把客户端连接轮询分配给 3 个 `sub_reactor`。
- **sub_reactor**：每个运行在自己的线程中，`epoll_wait` 监听 fd 的读/写事件；事件循环**直接面向 `connection`**（维护 fd → connection 映射）；收到数据后追加进该 `connection` 的接收缓冲，再把业务处理交给线程池。
- **线程池**：执行 `connection::process_incoming()`（切帧）→ `client_session::on_message()`（业务），避免阻塞 IO 线程。

### 5.2 一条消息的完整处理链路

1. `sub_reactor::loop` 通过 epoll 收到某个 fd 的读事件，取出 `connection`。
2. `read_data` 读取原始字节流。
3. `pool_add_task` 把字节流 `append_raw_data` 进 `connection` 的接收缓冲，并把 `process_incoming` 提交到线程池。
4. `connection::process_incoming` 按帧协议循环 `receiver::process_recv_data` 切出所有完整 JSON 消息（自动处理粘包/半包）。
5. 每切出一条完整消息，回调 `client_session::on_message`：解析 `type` 字段，通过 `message_handler` 策略分发调用对应业务。
6. 业务处理完成后调用 `package_message`（经弱引用转发到 `connection::package_message`）打包响应，写入 `sender` 发送缓冲区。
7. `sub_reactor` 收到对方的 EPOLLOUT 事件时调用 `connection::handle_write()` → `sender->send_msg()` 真正写回客户端。

---

## 六、核心模块说明

### 6.1 `client_session`（连接会话，方案 3b 重拆后为纯"会话 + 业务"）

一个客户端连接对应一个 `client_session`：
- `client_session` 不再持有/拥有任何网络收发模块，退化为**纯"会话 + 业务"**。
- 它只保存会话状态（`session_key_`、`online`、`current_account_`、`social_manager_`）与消息处理器 `handlers_`。
- 通过 **弱引用**（`std::weak_ptr<connection> conn_`）回指对应的 `connection`，需要发消息时 `conn_.lock()` 获取，避免悬垂指针（连接断开但会话仍在 `session_manager` 时，发消息会安全跳过）。
- 提供业务方法：`register_user`、`login`、`logout`、`kick_offline`、`exit_self`、`private_chat`、`group_chat`、`create_group`、`delete_group`、`group_add_client`、`group_delete_client`、`modify_group_name`、`show_chatlist`、`send_friend_request` 等。
- `on_message(json_data, file_data)`：由 `connection::process_incoming` 回调，仅负责解析 JSON 并按 `type` 策略分发到 `handlers_`。其中 `type=="heartbeat"` 时不进入业务，立即回 `heartbeat_ack`（`content` 为 `pong`）。
- `package_message(message, type)`：内部通过 `conn_.lock()` 转发到 `connection::package_message` 完成打包入发送缓冲。

### 6.2 `connection`（纯网络收发层，方案 3b 重拆）

在 3b 下，`connection` 被从 `client_session` 中彻底拆出、**拥有独立生命周期**，并由 `sub_reactor` 的事件循环**直接面向它维护**：
- 拥有底层 `fd` 与 `epoll_fd`，持有 `receiver_` / `sender_`，析构时负责关闭 fd。
- **供 epoller 调用**：`append_raw_data`（写入接收缓冲）、`process_incoming`（从中切出所有完整帧并交给会话业务）、`handle_write`（EPOLLOUT 时真正 `send_msg`）。
- **供业务层调用**：`package_message`（打包并入发送缓冲）、`send_file` / `upload_file`（文件收发）。
- 对象关系（无 shared_ptr 环）：`sub_reactor`→`connection`→`client_session` 单持有链；会话用 `weak_ptr<connection>` 回指。
- `client_session::package_message` 与 `on_message` 的对外签名在 3b 中收敛为「业务只认识会话，网络只认识 connection」。

### 6.2 `message_handler`（消息策略分发）

利用多态将 `type` 分发给不同处理器，避免 `handle` 中堆叠大量 `if-else`：
- `Chat_handler`：私聊 / 群聊 / 加好友
- `Group_handler`：建群 / 解散 / 加人 / 踢人 / 改名
- `Base_handler`：show / logout / exit / login / register
- `File_handler`：文件上传 / 下载

### 6.3 `receiver` / `sender`（收发与帧解析）

- **receiver**：维护接收缓冲区 `in_buffer`，`process_recv_data` 根据帧头长度字段做粘包/半包解析，得到「JSON 部分 + 文件数据部分」。
- **sender**：维护发送缓冲区 `out_buffer`，`add_to_out_buffer` 追加待发送数据，`send_msg` 用非阻塞 `send` 循环发送，并管理 epoll 写事件的注册/取消。

### 6.4 `session_manager`（会话管理，单例）
保存「会话 key → client_session」映射。登录前以 fd 为 key，登录后切换到 UID 作为 key，从而支持按 UID 精确转发私聊/群聊。

关于登录/登出的 key 切换与单账号管控：
- **登录**：先把 `key` 从 fd 切到 UID；若该 UID 已有在线会话，则先将其**顶下线**（`kick_offline`：发送"被顶下线"提示并关闭其旧连接），再登记新会话，保证**同一账户同一时间只允许一个在线 session**。
- **登出**（`logout`）：保留连接，把 `key` 还原为 fd，清空账号与社交状态，连接可继续登录其他账号；断线清理（`exit`）则从映射中移除。

### 6.5 `NoticeService`（全局通知服务 / 依赖倒置的"端口"）
业务层（`logic/group.cpp`、`logic/social_module.cpp`）不再直接依赖网络层的 `session_manager` / `client_session` 来发消息，而是统一通过 `NoticeService` 这一端口完成"向在线用户发消息"：
- `send_to_user(uid, msg, type)`：向指定 UID 用户发一条消息（内部经 `session_manager` 找到连接并 `package_message`）。
- `send_to_users(uid_list, msg, type)`：向一组 UID 广播（自动忽略不在线的）。
- `is_online(uid)`：查询某 UID 当前是否在线。
> 实现细节封装在 `notice_service.cpp`，头文件只暴露面向业务的语义化接口，从而把 `logic → net_core` 的依赖收敛到单个端口。

### 6.6 账户/群组/社交模块与 UID 分配

**会话模块（非单例，被 `client_session` 直接持有）：**
- `account`（`current_account_`）：当前登录用户的账户数据，登录时经 repo 加载并填充，登出/断开随之释放。
- `social_module`（`social_manager_`）：当前登录用户的社交关系（好友/群组列表）。同样是每个已登录 `client_session` **直接持有**的成员（登录时 `std::make_shared`），跨用户通知统一走 `NoticeService`，不需要全局映射（已取消多余的 `social_manager` 单例）。

**单例（保留）：**
- `group_manager`：**保留**。群聊需要"有成员在线就维持、无成员就关闭"的活跃内存生命周期管理，与账户不同，无法仅靠数据库承载该状态。
- `UID_allocator`：ID 分配器。接入数据库后，其初始值由数据库 `MAX(UID)/MAX(group_UID)` 重新定位，避免重启撞号（详见《数据库设计.txt》）。

**`account_manager`（废弃）：**
- 原全局账户表单例已**废弃**。它的存在只是当年没有数据库、需要借别人账户对象查昵称的妥协产物。接入 MySQL 后，按 UID 查账户资料应直接查库，因此该 manager 失去意义，将被移除。

### 6.7 数据库与 repo 分层（引入持久化）

引入 MySQL 后，新增 `db/` 分层，核心约束：

```
net_core 业务会话层 (client_session / social_module ...)
        │  只 include repo_interface，面向接口编程
        ▼
db/repo_interface  (纯接口/契约 + 空实现占位，无 SQL)
        │  实现接口
        ▼
db/repo            (真正的 MySQL 实现，SQL 全部在此)
        │  使用连接池
        ▼
db/mysql           (MySQL 连接池 mysql_conn_pool)
```

**硬性规则：**
1. **SQL 语句只能出现在 `src/db/repo/` 目录**；其他目录禁止出现 SQL 与 mysql 头文件。
2. **业务层只能 include `repo_interface`，不能 include `repo`**，面向接口编程，不感知 MySQL 细节。
3. **repo 只实现 repo_interface 声明的接口**，并发经 `db/mysql` 的 mysql_conn_pool 分配连接。
4. **迁移期用空实现占位**：业务调用点先全部迁到 repo_interface 接口上，接口的默认实现暂不真正访问库；接入 MySQL 后替换真实实现，上层代码无需再动。

**MySQL 连接池（`db/mysql/mysql_conn_pool.h/.cpp`）**：
- **单例** `MySQL_Conn_Pool::get_instance()`，供 `db/repo` 层获取/归还数据库连接，所有 SQL 连接统一经此分配。
- `init()`：服务器启动时（`main.cpp` 加载配置后）调用，按 `configure.json` 的 `database` 配置（`host`/`port`/`user`/`password`/`dbname`/`db_conn_count`）预建 `db_conn_count` 个连接。
- `get_connection()`：优先复用空闲连接；不足且未达上限时临时新建；**已达上限则阻塞等待**空闲连接归还。DB 不可用创建失败时返回 `nullptr`，由上层决定重试策略（不崩溃）。
- `return_connection(conn)`：用完必须归还，否则连接泄漏。`shutdown()`：关闭并清空连接池。
- `db_conn_count` 为连接池上限，默认 4（见 `configure.json`）。

**repo 接口（`I_account_repo`）** 规划：
- `register_account(name, pwd)` 注册（返回新账户对象）
- `remove_account(uid)` 注销
- `load_account(uid)` 加载（登录加载自己 / social 查他人资料）
- `update_account(acc)` 修改账号个人数据

通过**构造注入**下发给 `client_session`，再由 `client_session` 传给 `social_module`，使二者获得"访问对应文件接口"的能力。

### 6.8 客户端缓存 + 变更推送（规划）
- 客户端本地缓存头像、昵称等展示数据。
- 服务器在账户资料变更时推送"某 UID 资料已变更"，或客户端定期比对版本，触发重新查库刷新。
- 目标：降低对数据库的重复查询压力。详见《开发日志.txt》未实现目标。

---

## 七、通讯协议说明

- **传输**：TCP
- **帧格式**：`4字节总长度 | 4字节JSON长度 | JSON | 文件数据(可选)`
  - 长度字段为网络字节序（`htonl` / `ntohl`）
- **消息体**：JSON，必须以 `type` 表明消息类型
- **寻址**：基于整数 UID（用户与群组共用同一套编号空间）

支持的消息类型：`register`、`login`、`logout`、`show`、`exit`、`heartbeat`、`private_chat`、`group_chat`、`create_group`、`delete_group`、`group_add_client`、`group_delete_client`、`modify_group_name`、`upload_file`、`download_file`。

详细的字段定义与返回格式见 **`客户端接口文档.txt`**。

---

## 八、已实现功能

- ✅ 用户注册、登录（注册后回传分配到的 UID）
- ✅ 登出（`logout`，保留连接可再登录）与安全退出（`exit`，断开连接）
- ✅ 单账号管控：同一账户重复登录时，后登录顶掉先登录（旧连接收到提示后下线）
- ✅ 心跳包检测（可配项，`configure.json` 控制开关与超时阈值）
- ✅ 个人私聊（按 UID 精确送达）
- ✅ 群聊的创建 / 解散 / 加人 / 踢人 / 改名，含管理员权限校验
- ✅ 好友系统（加好友、好友/群组列表展示）
- ✅ 文件上传（分块）与下载（按 `file_id` 重组）
- ✅ TCP 粘包 / 半包处理
- ✅ 多线程主从 Reactor + 线程池

- ✅ 数据库连接池（MySQL）：`db/mysql` 连接池已实现并在启动时初始化，支持按 `configure.json` 的 `database` 配置预建受限连接、复用/归还。表结构见 `sql/create_table.sql`，repo 分层设计与迁移方案见 §6.7；`db/repo` 层真正的 SQL 持久化实现仍在进行中

---

## 九、开发日志

详见 **`开发日志.txt`**（包含历史待办项、已完成项及分次改动记录）。

---

## 十、常见问题

**Q1：`server` 启动后无反应？**
- 检查 `build/server` 是否已生成；未生成先执行 `cd build && cmake --build . -j8`。

**Q2：提示找不到 nlohmann/json？**
- 安装依赖：`sudo apt install nlohmann-json3-dev`。

**Q3：客户端登录后收不到响应？**
- 请确认客户端遵循本仓库的**帧协议与字段命名**（见「客户端接口文档.txt」），尤其是登录字段为 `UID`（整数）与 `password`。

**Q4：文件保存在哪？**

- 服务器接收文件默认保存到工作目录下的 `received_files/`（见宏 `SERVER_SAVING_PATH`）。
