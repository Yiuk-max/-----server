# 项目说明文档

## 一、项目简介

本项目是一个**基于 epoll + 主从 Reactor（Main-Sub Reactor）多线程模型的 TCP 聊天服务器**。
- 支持**用户注册/登录**、**私聊**、**群聊**、**好友系统**、**文件传输**等功能。
- 实现了**自定义二进制帧协议**（定长包头 + JSON 消息体 + 可选文件数据），能够正确处理 TCP **粘包 / 半包**。
- 采用**单例模式**管理多个全局模块（账户、群组、会话、社交关系、UID 分配器）。
- 使用 **C++17** 与 **nlohmann/json** 解析网络消息。

> 本仓库仅包含**服务器端**源代码，客户端需要自行实现或使用现有测试客户端连接调试。

---

## 二、技术栈

| 项目 | 说明 |
|------|------|
| 开发语言 | C++17 |
| 网络模型 | epoll + 主从 Reactor 多线程 |
| 并发模型 | 1 个 Main Reactor（accept）+ 3 个 Sub Reactor（IO 事件）+ 线程池（8 线程处理业务） |
| JSON 库 | nlohmann/json |
| 构建工具 | CMake |
| 数据持久化 | MySQL Connector/C++（头文件引用，SQL 目录为占位示例，持久化尚未完整实现） |

---

## 三、目录结构

```
server/
├── CMakeLists.txt                    # CMake 构建配置
├── src/
│   ├── main.cpp                      # 服务器入口：创建 socket、主从 Reactor、线程池
│   ├── net_core/                     # 网络核心层
│   │   ├── epoller.h/.cpp            # 主从 Reactor：main_reactor / sub_reactor

│   │   ├── connection.h/.cpp         # 纯网络收发层（3b 重拆；receiver/sender/帧解析/打包/接收驱动）
│   │   ├── client_session.h/.cpp     # 单个连接会话：登录、私聊、群聊、文件等业务
│   │   ├── message_handler.h/.cpp    # 消息策略分发（多态，Chat/Group/Base/File）
│   │   ├── notice_service.h/.cpp     # 全局通知服务（业务层发消息的"端口"）
│   │   ├── receiver_sender.h/.cpp    # 帧协议解析与发送（sender / receiver）
│   │   └── session_manager.h/.cpp    # 全局会话管理（单例，UID/句柄映射）
│   ├── logic/                        # 业务逻辑层
│   │   ├── account.h/.cpp            # 用户账户信息
│   │   ├── account_manager.h/.cpp    # 账户管理（单例）
│   │   ├── group.h/.cpp             # 群聊
│   │   ├── group_manager.h/.cpp      # 群聊管理（单例）
│   │   ├── social_module.h/.cpp      # 社交关系模块（好友/群组列表）
│   │   ├── social_module_manager.h/.cpp    # 社交关系管理（单例）
│   │   └── UID_allocator.h/.cpp      # 用户/群聊ID分配器（单例）
│   └── utils/
│       └── thread_pool.h/.cpp        # 线程池
├── include/
│   └── total.h                       # 公共头文件（类型、常量、全局声明）
├── SQL/
│   └── create_table.sql              # 数据库占位脚本（尚未完善）
├── build/                            # 构建输出目录（已配置过 CMake）
├── 客户端接口文档.txt                  # 前后端 JSON/帧协议接口说明
└── to_do.txt                         # 待办清单与开发日志
```

---

## 四、启动与编译

### 4.1 依赖

- CMake ≥ 3.10
- 支持 C++17 的编译器
- nlohmann/json 库（`sudo apt install nlohmann-json3-dev`）
- MySQL Connector/C++（当前仅在需要访问数据库的文件中按需引入，`total.h` 不再强制依赖 `<mysql/jdbc.h>`）

### 4.2 首次配置与编译

```bash
# 第一次：配置
cd build
cmake ..

# 编译（此后每次改动只需执行这一条）
cd build
cmake --build .
```

### 4.3 运行服务端

```bash
cd build
./server
```

服务器启动后监听** IPv6 通配地址** `:8080`。

> 说明：项目使用 IPv6 socket（`AF_INET6`），`in6addr_any` 兼容接收 IPv4 与 IPv6 连接。

---

## 五、架构与工作流程

### 5.1 主从 Reactor 模型

```
                    ┌────────────────────┐
                    │  main_reactor        │
                    │  (epoll_wait)        │
                    │  accept 新连接        │
                    └─────────┬──────────┘
                              │ 按负载轮流分配
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
      ┌────────────┐  ┌────────────┐  ┌────────────┐
      │  sub1       │  │  sub2       │  │  sub3       │
      │  epoll loop │  │  epoll loop │  │  epoll loop │
      └─────┬──────┘  └─────┬──────┘  └─────┬──────┘
            │  直接面向 connection           │
            └───────────────┼───────────────┘
                            ▼
                   ┌──────────────┐
                   │ connection:   │  持有接收缓冲/帧解析/发送
                   │ ThreadPool    │  (8 workers) process_incoming → session
                   └──────────────┘
```

- **main_reactor**：负责 `accept`，把客户端连接轮询分配给 3 个 `sub_reactor`。
- **sub_reactor**：每个运行在自己的线程中，`epoll_wait` 监听 fd 的读/写事件；事件循环**直接面向 `connection`**（维护 fd → connection 映射）；收到数据后追加进该 `connection` 的接收缓冲，再把业务处理交给线程池。
- **线程池**：执行 `connection::process_incoming()`（切帧）→ `client_session::on_message()`（业务），避免阻塞 IO 线程。

### 5.2 一条消息的完整处理链路

1. `sub_reactor::loop` 通过 epoll 收到某个 fd 的读事件，取出 `connection`。
2. `read_data` 读取原始字节流。
3. `pool_add_task` 把字节流 `append_raw_data` 进 `connection` 的接收缓冲，并把 `process_incoming` 提交到线程池。
4. `connection::process_incoming` 按帧协议循环 `receiver::process_recv_data` 切出所有完整 JSON 消息（自动处理粘包/半包）。
5. 每切出一条完整消息，回调 `client_session::on_message`：解析 `type` 字段，通过 `message_handler` 策略分发调用对应业务。
6. 业务处理完成后调用 `package_message`（经弱引用转发到 `connection::package_message`）打包响应，写入 `sender` 发送缓冲区。
7. `sub_reactor` 收到对方的 EPOLLOUT 事件时调用 `connection::handle_write()` → `sender->send_msg()` 真正写回客户端。

---

## 六、核心模块说明

### 6.1 `client_session`（连接会话，方案 3b 重拆后为纯"会话 + 业务"）

一个客户端连接对应一个 `client_session`：
- `client_session` 不再持有/拥有任何网络收发模块，退化为**纯"会话 + 业务"**。
- 它只保存会话状态（`session_key_`、`online`、`current_account_`、`social_manager_`）与消息处理器 `handlers_`。
- 通过 **弱引用**（`std::weak_ptr<connection> conn_`）回指对应的 `connection`，需要发消息时 `conn_.lock()` 获取，避免悬垂指针（连接断开但会话仍在 `session_manager` 时，发消息会安全跳过）。
- 提供业务方法：`register_user`、`login`、`logout`、`exit_self`、`private_chat`、`group_chat`、`create_group`、`delete_group`、`group_add_client`、`group_delete_client`、`modify_group_name`、`show_chatlist`、`send_friend_request` 等。
- `on_message(json_data, file_data)`：由 `connection::process_incoming` 回调，仅负责解析 JSON 并按 `type` 策略分发到 `handlers_`。
- `package_message(message, type)`：内部通过 `conn_.lock()` 转发到 `connection::package_message` 完成打包入发送缓冲。

### 6.2 `connection`（纯网络收发层，方案 3b 重拆）

在 3b 下，`connection` 被从 `client_session` 中彻底拆出、**拥有独立生命周期**，并由 `sub_reactor` 的事件循环**直接面向它维护**：
- 拥有底层 `fd` 与 `epoll_fd`，持有 `receiver_` / `sender_`，析构时负责关闭 fd。
- **供 epoller 调用**：`append_raw_data`（写入接收缓冲）、`process_incoming`（从中切出所有完整帧并交给会话业务）、`handle_write`（EPOLLOUT 时真正 `send_msg`）。
- **供业务层调用**：`package_message`（打包并入发送缓冲）、`send_file` / `upload_file`（文件收发）。
- 对象关系（无 shared_ptr 环）：`sub_reactor`→`connection`→`client_session` 单持有链；会话用 `weak_ptr<connection>` 回指。
- `client_session::package_message` 与 `on_message` 的对外签名在 3b 中收敛为「业务只认识会话，网络只认识 connection」。

### 6.2 `message_handler`（消息策略分发）

利用多态将 `type` 分发给不同处理器，避免 `handle` 中堆叠大量 `if-else`：
- `Chat_handler`：私聊 / 群聊 / 加好友
- `Group_handler`：建群 / 解散 / 加人 / 踢人 / 改名
- `Base_handler`：show / exit / login / register
- `File_handler`：文件上传 / 下载

### 6.3 `receiver` / `sender`（收发与帧解析）

- **receiver**：维护接收缓冲区 `in_buffer`，`process_recv_data` 根据帧头长度字段做粘包/半包解析，得到「JSON 部分 + 文件数据部分」。
- **sender**：维护发送缓冲区 `out_buffer`，`add_to_out_buffer` 追加待发送数据，`send_msg` 用非阻塞 `send` 循环发送，并管理 epoll 写事件的注册/取消。

### 6.4 `session_manager`（会话管理，单例）
保存「会话 key → client_session」映射。登录前以 fd 为 key，登录后切换到 UID 作为 key，从而支持按 UID 精确转发私聊/群聊。

### 6.5 `NoticeService`（全局通知服务 / 依赖倒置的"端口"）
业务层（`logic/group.cpp`、`logic/social_module.cpp`）不再直接依赖网络层的 `session_manager` / `client_session` 来发消息，而是统一通过 `NoticeService` 这一端口完成"向在线用户发消息"：
- `send_to_user(uid, msg, type)`：向指定 UID 用户发一条消息（内部经 `session_manager` 找到连接并 `package_message`）。
- `send_to_users(uid_list, msg, type)`：向一组 UID 广播（自动忽略不在线的）。
- `is_online(uid)`：查询某 UID 当前是否在线。
> 实现细节封装在 `notice_service.cpp`，头文件只暴露面向业务的语义化接口，从而把 `logic → net_core` 的依赖收敛到单个端口。

### 6.6 `account_manager` / `group_manager` / `social_manager` / `UID_allocator`
均为**单例模式**，分别管理账户、群组、社交关系、ID 分配，内部使用互斥锁保护共享数据。

---

## 七、通讯协议说明

- **传输**：TCP
- **帧格式**：`4字节总长度 | 4字节JSON长度 | JSON | 文件数据(可选)`
  - 长度字段为网络字节序（`htonl` / `ntohl`）
- **消息体**：JSON，必须以 `type` 表明消息类型
- **寻址**：基于整数 UID（用户与群组共用同一套编号空间）

支持的消息类型：`register`、`login`、`show`、`exit`、`private_chat`、`group_chat`、`create_group`、`delete_group`、`group_add_client`、`group_delete_client`、`modify_group_name`、`upload_file`、`download_file`。

详细的字段定义与返回格式见 **`客户端接口文档.txt`**。

---

## 八、已实现功能

- ✅ 用户注册、登录（注册后回传分配到的 UID）
- ✅ 个人私聊（按 UID 精确送达）
- ✅ 群聊的创建 / 解散 / 加人 / 踢人 / 改名，含管理员权限校验
- ✅ 好友系统（加好友、好友/群组列表展示）
- ✅ 文件上传（分块）与下载（按 `file_id` 重组）
- ✅ TCP 粘包 / 半包处理
- ✅ 多线程主从 Reactor + 线程池
- ⚠️ 数据库持久化（MySQL）——头文件已引用连接库，但未开始实现

---

## 九、开发日志

详见 **`to_do.txt`**（包含历史待办项、已完成项及分次改动记录）。

---

## 十、常见问题

**Q1：`server` 启动后无反应？**
- 检查 `build/server` 是否已生成；未生成先执行 `cd build && cmake --build . -j8`。

**Q2：提示找不到 nlohmann/json？**
- 安装依赖：`sudo apt install nlohmann-json3-dev`。

**Q3：客户端登录后收不到响应？**
- 请确认客户端遵循本仓库的**帧协议与字段命名**（见「客户端接口文档.txt」），尤其是登录字段为 `UID`（整数）与 `password`。

**Q4：文件保存在哪？**

- 服务器接收文件默认保存到工作目录下的 `received_files/`（见宏 `SERVER_SAVING_PATH`）。
